# This is a library of functions and code, just to remember!
x <- rnorm(1000)
str(x)  # shows the structure of the object
colMeans(Report1) # All the columns must be numeric
colSums(x)

# ifelse
# data$stepsNew <- ifelse(is.na(data$steps),
#                         mean_interval[as.character(data$interval[which(is.na(data$steps))])],
#                         data$steps)
